/ As part of CT-223 controls to accept / reject requests will be added
= render partial: 'layouts/header', locals: {page_heading: @case.name}

.grid-row
  .column-two-thirds
    p.lede
      span.visually-hidden = t('.subject')
      = @case.subject

.grid-row
  .column-two-thirds
    h2.form-label-bold = t('.request')
    .wrapper
      = @case.message
