$('#case-<%= @case.id %> .js-take-case-on-container').html(
    "<%= escape_javascript(
           render partial: 'take_case_on_or_de_escalate',
                  locals: { kase: @case }
         ) %>"
);

