

// if user has not searched
  // return "No linked cases"


// If they have added one case
 // set as original
$('.case-linking .column-full tbody')
.append(" <%= j render partial: 'cases/ico/linked_cases', locals: { linked_case: linked_case } %>")


// if they have move than one search then related cases




