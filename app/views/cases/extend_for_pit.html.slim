- content_for :heading
  = t('.extend_for_pit')

- content_for :sub_heading
  span.visually-hidden
    = t('common.case.header_case_number')
  = @case.number

= render partial: 'layouts/header'
br

div.action-copy
  p
    = "You are extending the time on:"
    strong.strong-block
      = @case.subject
  p.notice
    i class="icon icon-important"
      span.visually-hidden
        = "Important"
    strong.normal-weight
      = "The additional time can't be used to determine whether exemptions apply. It must already be identified that a qualified exemption applies."

br

= form_for @case, url: execute_extend_for_pit_case_path(@case) do |f|
  .form-group
    = f.gov_uk_date_field :external_deadline,
                          { legend_text: t('cases.extend_for_pit.extension_date'),
                            form_hint_text: t('cases.extend_for_pit.extension_date_hint') }


  = f.text_area :reason_for_extending

  .actions
    = submit_tag 'Submit', class: 'button'
