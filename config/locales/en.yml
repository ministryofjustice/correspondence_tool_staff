# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t 'hello'
#
# In views, this is aliased to just `t`:
#
#     <%= t('hello') %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more, please read the Rails Internationalization guide
# available at http://guides.rubyonrails.org/i18n.html.

en:
  activerecord:
    errors:
      models:
        case:
          attributes:
            email:
              blank: and address can't both be blank
            postal_address:
              blank: and email can't both be blank
            reference:
              taken: An error has occurred and your case could not be created.  Please try again.
    attributes:
      case:
        reference: ""

  date:
    formats:
      default: "%d/%m/%Y"
      deadline: "%d %b"
  time:
    formats:
      default: "%d/%m/%Y %H:%M"
      external_deadline: "%d %b"
      deadline: "%d %b"
  button:
    continue: Continue
    confirm: Confirm
  helpers:
    fieldset:
       assignment:
          state: Do you accept this case?

    hint:
      assignment:
        reasons_for_rejection: |
          Try including who you think with right division may be
    label:
      case:
        user_id: 'Drafter'
        name: Full name
        subject: Subject of request
        message: Full request
        postal_address: Address
      assignment:
        reasons_for_rejection: Reason for rejecting this case

        state:
          accepted: Accept
          rejected: Reject

  common:
      error: error
      phase_banner: |
        This is a new service – your feedback will help us to improve it.
      name: Full name
      message: Full request
      postal_address: Address
      summary_error: 'prevented this form from being submitted:'
      new_case_button: New case
  cases:
    acceptance:
      subject: 'Subject:'
      request: 'Request'
    index:
      page_heading: Cases
      requester: Requester name
      subject: Subject
      external_deadline: External deadline
      status: Status
    fixed_details:
      requester: Requester
      email_address: Email address
      escalation_deadline: Escalation deadline
      external_deadline: External deadline
      message: Full request
      details: Details
    new:
      new_case: New case
      received_date: Date received
      received_date_copy: For example. 31 01 2017
    create:
      case_created: Case successfully created
    update:
      case_updated: Case updated
    assign:
      case_assigned: Case assigned to #email_of_assignee
