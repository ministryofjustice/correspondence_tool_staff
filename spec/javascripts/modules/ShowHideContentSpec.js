describe('Modules.ShowHideContent.js', function() {
  it('should exist with expected constructures', function() {
    expect(moj.Modules.ShowHideContent.init).toBeDefined();
    expect(moj.Modules.ShowHideContent.cacheEls).toBeDefined();
    expect(moj.Modules.ShowHideContent.bindEvents).toBeDefined();
    expect(moj.Modules.ShowHideContent.elementSelector).toBeDefined();
  });
});
