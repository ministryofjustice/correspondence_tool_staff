# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignore bundler config.
/.bundle
/vendor/bundle

# Ignore all logfiles and tempfiles.
/tmp/*
/log/*
build

# Ignore concatenated state machine config file - it will be regenerated
# from the constituent parts
/config/state_machine/moj.yml

# Ignore Byebug command history file.
.byebug_history

config/settings.local.yml
config/settings/*.local.yml
config/environments/*.local.yml
.env
env.sh
.env.sh
.docker-sync
config/docker-dev/.setup-complete

#Ignore local upload and attachments
/uploads
public/uploads

# Ignore .DS_Store in all directories
.DS_Store
._DS_Store

public/assets/*
!public/assets/govuk-frontend.*

dump.rdb

node_modules

# Ignore original files left over from merge
*.orig

# ignore local coverage files
coverage

# Ignore files that may contain production data. If you need to allow specific
# files include them below using the ! notation.
**/*.bak
**/*.sql
**/*.csv
**/*.txt
**/*.xls
**/*.xlsx
**/*.tar

# exceptions to the above
!db/structure.sql
!public/robots.txt
!lib/assets/kilo_map.csv
!spec/fixtures/*.xlsx
!spec/fixtures/*.txt
!tmp/parallel_runtime_rspec.log

# Generated documentation.
doc
.yardoc

# ignore secret files
*secret*.*

.idea/
/config/docker-dev/shared/pgadmin/storage/
!/config/docker-dev/shared/pgadmin/storage/cts_pgadmin.com/*
